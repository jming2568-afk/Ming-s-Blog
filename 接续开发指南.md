# 接续开发指南

> 李佳铭个人博客（Ming-s-Blog）项目状态与后续开发筹备
> 更新时间：2026-08-12

---

## 一、项目概况

| 项 | 内容 |
|---|---|
| 项目名称 | Ming-s-Blog（李佳铭个人博客） |
| 定位 | 个人作品集网站：简历展示 + AIGC 漫剧作品集 + 全栈开发项目展示 |
| 技术栈 | Next.js 16.2.6（App Router）+ React 19.2.4 + Tailwind CSS v4 + framer-motion |
| 包管理器 | npm 10.8.2 |
| Node 基线 | 22.22.2（managed） |
| GitHub 仓库 | https://github.com/jming2568-afk/Ming-s-Blog（Public） |
| 自定义域名 | subming.top（DNS 已配置 A 记录 → 76.76.21.21） |
| Vercel 部署 | 待完成（仓库已就绪，需在 vercel.com 导入部署） |

---

## 二、已完成工作

### 2.1 博客搭建（2026-08-12）
- 手动创建 Next.js 16 项目结构（非 create-next-app，更可控）
- 4 个页面全部验证通过（HTTP 200）：
  - `/` 首页：Hero + 数据亮点 + 精选作品 + 技能概览 + 时间线
  - `/resume` 简历：AIGC 漫剧版 / 全栈开发版双版本切换 + 打印支持
  - `/portfolio` 作品集：分类筛选（全部/AIGC漫剧/开发项目）+ 卡片网格
  - `/contact` 联系方式：仅邮箱 + GitHub（不放手机/微信，防爬虫）
- 响应式布局：移动端汉堡菜单 + 平板/桌面多列
- data/ 集中管理内容，修改数据文件即可更新网站

### 2.2 配套产出（同日完成）
- **工作区总览 README**：`工作\README.md`
- **主项目技术 README**：`优阅漫剧工作台\AI-studio-youyue-manju\README.md`（maint:readme:check 通过）
- **AIGC 漫剧版简历**：`个人简历\李佳铭_简历_AIGC漫剧方向_优化版.md`
- **全栈开发版简历**：`个人简历\李佳铭_简历_全栈开发方向_优化版.md`

### 2.3 Git 与 GitHub
- 本地 git 初始化，分支 `main`
- git config（本地）：user.name=`jming2568-afk`，user.email=`jming2568@gmail.com`
- GitHub CLI 安装（v2.97.0，winget 安装），已 `gh auth login`
- 代码已推送到 github.com/jming2568-afk/Ming-s-Blog
- `.gitignore` 已完善（node_modules / .next / .env / Thumbs.db / IDE 文件）

### 2.4 DNS 配置
- 域名：subming.top（火山引擎 TrafficRoute DNS）
- 已添加 A 记录：主机记录 `@` → 记录值 `76.76.21.21`（Vercel 官方 IP）
- TTL：10 分钟

---

## 三、项目结构

```
个人博客/
├── app/
│   ├── layout.js          # 全局布局（NavBar + Footer）
│   ├── page.js            # 首页
│   ├── globals.css        # Tailwind v4 入口 + 自定义样式 + 打印样式
│   ├── resume/page.js     # 简历页（ResumeTabs 组件）
│   ├── portfolio/page.js  # 作品集页（ProjectFilter 组件）
│   └── contact/page.js    # 联系页
├── components/
│   ├── NavBar.jsx         # 顶部导航（client，移动端汉堡菜单）
│   ├── Footer.jsx         # 页脚
│   ├── HeroSection.jsx    # 首页 Hero（client，framer-motion 动画）
│   ├── SectionTitle.jsx   # 通用章节标题
│   ├── ProjectCard.jsx    # 作品卡片（client，framer-motion hover）
│   ├── ProjectFilter.jsx  # 分类筛选（client，useState）
│   ├── ResumeTabs.jsx     # 简历双版本切换（client，useState + AnimatePresence）
│   ├── Timeline.jsx       # 经历时间线
│   └── SkillBar.jsx       # 技能进度条
├── data/
│   ├── profile.js         # 个人信息（仅邮箱+GitHub）
│   ├── resume.js          # 两版简历结构化数据
│   ├── projects.js        # 作品集数据（漫剧6部 + 开发4个）
│   ├── skills.js          # 技能分类数据（5类）
│   └── timeline.js        # 经历时间线（4段）
├── lib/utils.js           # cn() 类名合并工具
├── public/images/avatar.svg  # 头像占位
├── package.json
├── next.config.mjs
├── postcss.config.mjs     # @tailwindcss/postcss
├── jsconfig.json          # 路径别名 @/* → ./*
├── .gitignore
└── README.md              # 博客项目说明
```

---

## 四、本地开发命令

### 环境要求
- Node.js 22.22.2（managed：`C:\Users\jming\.workbuddy\binaries\node\versions\22.22.2\node.exe`）
- 或系统 Node 24.19.0（fallback）

### 启动开发服务器
```bash
cd "C:/Users/jming/Documents/Project/工作/个人博客"
npm run dev
# 访问 http://localhost:3000
```

### 用 managed node 直接运行（无需系统 npm）
```bash
cd "C:/Users/jming/Documents/Project/工作/个人博客"
"C:/Users/jming/.workbuddy/binaries/node/versions/22.22.2/node.exe" node_modules/next/dist/bin/next dev
```

### 构建
```bash
npm run build    # next build
npm run start    # next start（生产模式）
```

---

## 五、Git 与 GitHub 操作

### 提交推送
```bash
cd "C:/Users/jming/Documents/Project/工作/个人博客"
git add -A
git commit -m "描述改动"
git push
```

### GitHub CLI（已认证）
```bash
# gh 全路径（PATH 可能未刷新）
"/c/Program Files/GitHub CLI/gh.exe" <命令>

# 常用
gh repo view                    # 查看仓库
gh repo view -w                 # 浏览器打开仓库
gh issue list                   # 查看 issue
```

### 远程仓库
- origin: https://github.com/jming2568-afk/Ming-s-Blog.git
- 默认分支: main
- 可见性: Public

---

## 六、Vercel 部署（待完成）

### 部署步骤
1. 浏览器打开 vercel.com（已创建账号，已连接 GitHub）
2. "Add New" → "Project"
3. 仓库列表找 "Ming-s-Blog" → "Import"
4. 配置页不用改 → "Deploy"
5. 等 1-2 分钟，获得 xxx.vercel.app 临时域名

### 绑定自定义域名
1. Vercel 项目 → Settings → Domains
2. 输入 `subming.top` → Add
3. Vercel 自动验证 DNS（A 记录 76.76.21.21 已配置）
4. 验证通过后自动配置 HTTPS 证书

### DNS 记录（已配置，备查）
| 主机记录 | 类型 | 记录值 | 服务商 |
|---|---|---|---|
| @ | A | 76.76.21.21 | 火山引擎 TrafficRoute DNS |

> 如需 www 子域名，另加 CNAME：`www` → `cname.vercel-dns.com`

---

## 七、内容维护指南

所有展示内容集中在 `data/` 目录，修改对应文件即可更新网站：

| 想改什么 | 改哪个文件 |
|---|---|
| 姓名/邮箱/GitHub/简介 | `data/profile.js` |
| 简历内容（工作经历/项目/技能） | `data/resume.js` |
| 作品集增删 | `data/projects.js` |
| 技能列表与熟练度 | `data/skills.js` |
| 经历时间线 | `data/timeline.js` |

改完后 `git add -A && git commit -m "更新内容" && git push`，Vercel 会自动重新部署。

---

## 八、待办与后续开发方向

### 近期（部署完成后）
- [ ] 完成 Vercel 部署 + 绑定 subming.top
- [ ] 验证 https://subming.top 可访问
- [ ] 补充真实作品截图（替换占位，注意版权脱敏）
- [ ] 替换头像 avatar.svg 为真实照片或设计头像

### 功能增强（可选）
- [ ] 项目详情页 `/projects/[slug]`（动态路由，展示单个项目详情）
- [ ] 深色模式切换（Tailwind dark mode + 主题按钮）
- [ ] 博客文章模块（MDX，用 next-mdx-remote）
- [ ] SEO 优化（metadata API + sitemap.xml + robots.txt）
- [ ] 访问统计（Vercel Analytics 或百度统计）
- [ ] 联系表单（接入 Vercel Serverless Function 或 Formspree）

### 内容补充
- [ ] 补充更多漫剧项目数据（经历项目表格里还有《开局掳走太子妃》《女蛇》《玄幻萌宝》等）
- [ ] 添加 Ignesis 项目的架构图或设计稿
- [ ] 考虑加"关于我"详细页（服役经历、转型故事等）

---

## 九、踩过的坑与经验

1. **framer-motion 必须加 "use client"**：使用 motion 的组件（如 ProjectCard）在 Next.js App Router 中必须是客户端组件，否则报 `Element type is invalid: got undefined`
2. **GitHub 创建仓库时关掉 README/.gitignore/License**：否则远程有初始 commit，本地推送需 `git push -f` 强制覆盖
3. **Tailwind v4 配置方式**：不用 tailwind.config.js，在 globals.css 用 `@import "tailwindcss"` + `@theme`，postcss.config 用 `@tailwindcss/postcss`
4. **managed node 的 npm 调用**：`node.exe node_modules/npm/bin/npm-cli.js install`（不要用 bare npm 命令）
5. **火山引擎 DNS**：配置域名解析用"公网域名管理"，不是"公共解析 PublicDNS"（两者是不同产品）

---

## 十、关联项目

| 项目 | 路径 | GitHub |
|---|---|---|
| 优阅漫剧助手（主项目） | `优阅漫剧工作台\AI-studio-youyue-manju\` | github.com/jming2568-afk/AI-studio-youyue-manju |
| 个人简历 | `个人简历\` | — |
| 经历项目素材 | `经历项目\` | —（版权属原公司，不入库） |

---

## 十一、关键路径速查

```
博客根目录    C:\Users\jming\Documents\Project\工作\个人博客\
工作区根目录  C:\Users\jming\Documents\Project\工作\
managed node  C:\Users\jming\.workbuddy\binaries\node\versions\22.22.2\node.exe
gh CLI        C:\Program Files\GitHub CLI\gh.exe
node workspace C:\Users\jming\.workbuddy\binaries\node\workspace（playwright-core 装在这里）
```

---

*本文件随项目进展持续更新。完成待办项后勾选，新增方向追加到对应章节。*
