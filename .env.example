# 用于 JWT Session 签名，生产部署必须设置（使用 `openssl rand -hex 32` 生成）
# 缺失会导致所有登录态在重启/重部署时失效，且有安全隐患。
AUTH_SECRET=

# 可选：SQLite 数据库文件绝对路径，默认 <cwd>/data/portfolio.db
# 注意：部署到 Vercel/Netlify 等 Serverless 环境时本地磁盘不可写，需要改用 Turso/Postgres 等。
DB_PATH=

# 可选：上传文件存储目录，默认 <cwd>/public/uploads
UPLOAD_DIR=
